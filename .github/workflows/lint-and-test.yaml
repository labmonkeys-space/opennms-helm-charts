name: lint and test

on:
  workflow_dispatch:
  pull_request:
    types: [ opened, synchronize, reopened ]
  push:
    branches:
      - '*'
    tags:
      - 'v*'  # Only version tags like v1.0.0

jobs:
  helm-lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # v5.0.1
        with:
          fetch-depth: 0

      - name: Setup Helm Lint Requirements
        uses: ./.github/actions/setup_helmlint
        with:
          create-kind-cluster: "true"

      - name: Run Helm Docs
        uses: losisin/helm-docs-github-action@2ccf3e77eb70dc80d62f8cc26f15d0a96b75fef4  # v1.8.0
        with:
          chart-search-root: ./charts
          git-push: true
          git-push-user-name: "GitHub Action"
          git-push-user-email: "twiki@labmonkeys.space"
          git-commit-message: "docs: Update Helm Readme files"

      - name: Run Chart-lint
        run: make lint

      - name: Resolve umbrella dependencies
        run: helm dep update charts/opennms-stack

      - name: Chart testing install Core chart
        run: make test-install-core

      - name: Chart testing install Minion chart
        run: make test-install-minion

      - name: Chart testing install Sentinel chart
        run: make test-install-sentinel

      - name: Chart testing install opennms-stack umbrella
        run: make test-install-stack
