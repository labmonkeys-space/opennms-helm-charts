apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "minion.fullname" . }}-test-connection"
  labels:
    {{- include "minion.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "minion.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
