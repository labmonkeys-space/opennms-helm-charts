apiVersion: v1
kind: Service
metadata:
  name: {{ include "minion.fullname" . }}
  labels:
    {{- include "minion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "minion.selectorLabels" . | nindent 4 }}
