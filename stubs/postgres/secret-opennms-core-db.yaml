---
# CI / lab-mode ONLY — DO NOT USE IN PRODUCTION.
# Plaintext credentials applied by `make install-postgres` to seed CNPG with
# the OpenNMS database user during chart-testing. Production deployments
# generate Secrets out-of-band.
apiVersion: v1
kind: Secret
metadata:
  namespace: default
  name: opennms-core-db
type: kubernetes.io/basic-auth
stringData:
  username: "opennms_dba"
  password: "Change_Me_OpenNMS_DBA"
