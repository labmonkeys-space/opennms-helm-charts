.DS_Store
.idea/
build/

# AI tool working directories
.claude/
.codex/

# Helm dependency archives — regenerated by `helm dep update`.
# Chart.lock is committed for reproducibility; the .tgz cache is not.
charts/*/charts/*.tgz

# OpenSpec — design artifacts kept locally only
openspec/
